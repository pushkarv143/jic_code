<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is OFF by default for every host. The two exceptions below exist
  only because the currently deployed backend terminates plain HTTP on an IP
  (see README > "Backend/API gaps", item 1).

  When TLS is put in front of the API:
    1. change prodApiUrl to https://...
    2. delete the 132.226.191.38 entry from this file
  Nothing else in the app needs to change.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <!-- Deployed API server (plain HTTP today). -->
        <domain includeSubdomains="false">132.226.191.38</domain>
        <!-- Local development: emulator host loopback + adb reverse. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
