<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is OFF by default for every host.

  The prod flavour now talks HTTPS to https://132-226-191-38.nip.io (Caddy
  terminates TLS in front of the backend), so it needs no exemption here and
  gets none.

  The IP entry below is retained solely for the qa flavour, which still points
  at http://132.226.191.38:8080 — bypassing the proxy is useful for testing the
  backend directly. Remove it if qa is ever moved to HTTPS too; prod does not
  depend on it.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <!-- qa flavour only: direct HTTP to the backend port, bypassing Caddy. -->
        <domain includeSubdomains="false">132.226.191.38</domain>
        <!-- Local development: emulator host loopback + adb reverse. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
